cmake_minimum_required(VERSION 2.8.3)
project (TermProject2020)
include_directories(./inc ./src)
aux_source_directory(./src SRC_LIST)
set(SRC ${SRC_LIST})
add_executable(build main.cpp ${SRC})
add_definitions(-std=c++11)
add_definitions(-O2)